# Inventario de Componentes: `lib/01_home`

Este documento contiene el inventario técnico estructurado de los archivos del subdirectorio [lib/01_home](file:///D:/buscobien/lib/01_home), encargado de gestionar el estado inmutable de navegación global y los flujos reactivos de las pestañas en Buscobien.

| Subdirectorio (si aplica) | Nombre del archivo | variables definidas en el archivo | clases | breve descripción de cada clase | variables de la clase | funciones o widgets definidos en la clase | breve descripción de cada función o widget en la clase | variables que utiliza | llamadas a otras clases o widgets |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| N/A | [home_state.dart](file:///D:/buscobien/lib/01_home/home_state.dart) | N/A | `HomeState` | Clase que define un estado inmutable para la navegación global en la aplicación. Se apoya en Freezed para ofrecer inmutabilidad segura, métodos de copia (`copyWith`) y seguimiento de versiones de renderizado. | - `int indiceInicial`<br>- `int indicePrincipal`<br>- `int indiceNivelGobierno`<br>- `int indiceTipoEspacio`<br>- `int indiceTipoTransaccion`<br>- `int indiceMiCuenta`<br>- `int indiceMiCuentaUsuario`<br>- `int version` | - Constructor factory `HomeState(...)`<br>- Constructor privado `const HomeState._()` <br>- Getters sobreescritos: `indiceInicial`, `indicePrincipal`, `indiceNivelGobierno`, `indiceTipoEspacio`, `indiceTipoTransaccion`, `indiceMiCuenta`, `indiceMiCuentaUsuario`, `version` | - Constructor por defecto que inicializa cada propiedad a cero por medio de `@Default(0)`.<br>- Constructor privado constante obligatorio para permitir la adición de métodos/getters personalizados.<br>- Getters abstractos requeridos por la estructura que lanzan `UnimplementedError` para ser sobreescritos por el código autogenerado. | N/A | - `_$HomeState` (Mixin generado por Freezed)<br>- `_HomeState` (Clase concreta de implementación generada)<br>- `UnimplementedError` |
| N/A | [home_navigation_provider.dart](file:///D:/buscobien/lib/01_home/home_navigation_provider.dart) | N/A | `HomeNavigation` | Controlador de estado interactivo (Notifier) de Riverpod que expone el estado global de navegación `HomeState` y provee métodos para mutar de forma síncrona y reactiva cada uno de los índices de las pestañas. | N/A (Administrado de forma interna por la propiedad reactiva `state` de Riverpod) | - `HomeState build()` <br>- `void setInicial(int i)` <br>- `void setPrincipal(int i)` <br>- `void setGobierno(int i)` <br>- `void setEspacio(int i)` <br>- `void setTransaccion(int i)` <br>- `void setMiCuenta(int i)` <br>- `void setMiCuentaUsuario(int i)` <br>- `void actualizarInicial(int index)` <br>- `void actualizarPrincipal(int index)` <br>- `void actualizarNivelGobierno(int index)` <br>- `void actualizarTipoEspacio(int index)` <br>- `void actualizarTipoTransaccion(int index)` <br>- `void actualizarMiCuenta(int index)` <br>- `void actualizarMiCuentaUsuario(int index)` | - Inicializa el estado con el índice inicial en 0.<br>- Asigna el índice inicial de navegación.<br>- Asigna el índice principal de navegación.<br>- Asigna el índice de nivel de gobierno.<br>- Asigna el índice de tipo de espacio.<br>- Asigna el índice de tipo de transacción.<br>- Asigna el índice de Mi Cuenta.<br>- Asigna el índice de Mi Cuenta de Usuario.<br>- Actualiza el índice inicial, incrementa la versión para forzar el renderizado y escribe logs de depuración.<br>- Actualiza el índice principal, incrementa la versión y escribe logs.<br>- Actualiza el índice de gobierno, incrementa la versión y escribe logs.<br>- Actualiza el índice de tipo de espacio, incrementa la versión y genera logs.<br>- Actualiza el índice de transacción, incrementa la versión e imprime logs.<br>- Actualiza el índice de Mi Cuenta, incrementa la versión y genera logs.<br>- Actualiza el índice secundario de Mi Cuenta Usuario, incrementa la versión y genera logs. | - `i`<br>- `index`<br>- `state` | - `_$HomeNavigation` (Clase base autogenerada)<br>- `HomeState`<br>- `debugPrintLevels` |
| N/A | `home_navigation_provider.g.dart` | - `final homeNavigationProvider = AutoDisposeNotifierProvider<HomeNavigation, HomeState>(...)` | `_$HomeNavigation` | Clase base abstracta autogenerada por Riverpod Generator que actúa como puente de tipos reactivos para el notifier `HomeNavigation`. | N/A | N/A | N/A | - `homeNavigationProvider` | - `AutoDisposeNotifierProvider`<br>- `HomeNavigation`<br>- `HomeState` |
| N/A | `home_state.freezed.dart` | - `T _$identity<T>(T value)` <br>- `const _privateConstructorUsedError = UnsupportedError(...)` | `_$_HomeState`, <br>`$HomeStateCopyWith` | Clases e interfaces autogeneradas por la biblioteca Freezed para dar soporte completo a la inmutabilidad de la clase `HomeState`, proveyendo copias seguras (`copyWith`), comparaciones por valor (`operator ==`), generación de hash y representaciones legibles de string (`toString`). | Campos internos de `HomeState` implementados en la clase concreta. | - `copyWith`<br>- `toString`<br>- `operator ==`<br>- `hashCode` | - Permite clonar de forma segura un objeto inmutable sustituyendo únicamente las propiedades deseadas.<br>- Retorna una cadena con la estructura del estado para facilitar la depuración.<br>- Compara instancias por su contenido en lugar de su referencia en memoria.<br>- Genera el identificador numérico único basado en el contenido. | Variables internas de serialización | - `UnsupportedError`<br>- `Map`<br>- `List` |
